# CLAUDE.md

This repo is a take-home assignment for Composio (AI Product Ops Intern). It is used for
initial shortlisting, so quality, accuracy and honesty matter more than anything else.

**Before doing anything, read `PROJECT_BRIEF.md` in full.** It is the spec, the plan and
the acceptance checklist. Follow its phases in order and stop at every 🛑 HUMAN GATE.

Engineering workflow: use the skills in `agent-skills/skills/` (see
`agent-skills/skills/using-agent-skills/SKILL.md`). Phase-to-skill mapping is in the brief.

Hard rules:
- Never invent a URL, quote, number or fact. Unknown is a valid answer.
- Never fill in `data/ground_truth.csv` yourself. A human does that.
- Never edit `data/results_v1.json` after it is frozen.
- Keep `tasks/todo.md` updated after every task.
- Commit after every completed task with a clear message.
